# CognitiveLoadDetection (OpenCV + Haar Cascades)

Run realtime detection:
python scripts/realtime_detection.py

Label windows during session:
press l (low), m (med), h (high)

Save session features:
press s

Train a model after collecting labeled CSVs:
python scripts/train_model.py

Install:
pip install -r requirements.txt
